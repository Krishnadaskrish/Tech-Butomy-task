import React from 'react';

function HomePageCard({ padding,bg }) {
  return (
    <div className={`mt-${padding} bg-${bg} w-56 h-[450px]  rounded-xl`}>
    </div>
  );
}

export default HomePageCard;
